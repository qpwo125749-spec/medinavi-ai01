# MediNavi AI 환경 설정 예시 파일
# 실제 사용 시 .env 파일로 복사하여 사용하세요
# 예: cp env.example .env

# ============================================
# 애플리케이션 기본 설정
# ============================================
APP_NAME=MediNavi AI
APP_VERSION=0.1.0
DEBUG=false
ENVIRONMENT=development  # development, staging, production

# ============================================
# 서버 설정
# ============================================
HOST=0.0.0.0
PORT=8000
WORKER_COUNT=1  # 프로덕션에서는 CPU 코어 수에 맞게 조정

# ============================================
# API 설정
# ============================================
API_V1_PREFIX=/api/v1

# ============================================
# 데이터베이스 설정 (향후 확장용)
# ============================================
# DATABASE_URL=postgresql://user:password@localhost/medinavi_db
# Deta Space 배포 또는 Atlas 사용 시 MongoDB 설정
MONGODB_URI=
MONGODB_DB=medinavi
MONGODB_COLLECTION=pubmed

# ============================================
# AI 모델 설정 (향후 확장용)
# ============================================
# OPENAI_API_KEY=your-openai-api-key-here
# AI_MODEL_NAME=gpt-3.5-turbo
# AI_TIMEOUT=30  # AI API 타임아웃 (초)
# AI_MAX_RETRIES=3  # AI API 재시도 횟수

# ============================================
# 다국어 설정
# ============================================
DEFAULT_LANGUAGE=ko
# SUPPORTED_LANGUAGES=ko,ja,en,de,fr,es,it

# ============================================
# 보안 설정
# ============================================
# 프로덕션에서는 반드시 강력한 SECRET_KEY를 설정하세요!
# 생성 방법: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-here-change-this-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=30

# API Key 인증 (헤더: X-API-Key)
# 기본 개발 키: dev-key (스테이징/프로덕션에서는 교체)
API_KEY=dev-key

# CORS 허용 출처 (쉼표로 구분)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
# GitHub Pages/프론트엔드 호스트 등 외부 출처 지정 시
CORS_ORIGINS=https://your-pages-or-localhost

# Rate Limiting
# SlowAPI 글로벌 제한 (전역): 60/min
RATE_LIMIT_GLOBAL=60/min
# 무거운 엔드포인트 전용 제한: 15/min (예: /medical/health-analysis)
RATE_LIMIT_HEAVY=15/min
RATE_LIMIT_PER_MINUTE=60  # (레거시) 분당 요청 제한
MAX_REQUEST_SIZE=10485760  # 최대 요청 크기 (바이트, 기본 10MB)

# ============================================
# 로깅 설정
# ============================================
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_FORMAT=json  # json 또는 text

# 연락처/외부 API 키 (선택)
CONTACT_EMAIL=you@example.com
NCBI_API_KEY=

# ============================================
# 캐싱 설정
# ============================================
ENABLE_CACHE=true
CACHE_TTL=3600  # 캐시 유효 시간 (초)
CACHE_MAX_SIZE=1000  # 최대 캐시 항목 수

# ============================================
# 성능 설정
# ============================================
REQUEST_TIMEOUT=30  # 요청 타임아웃 (초)

